# Required environment variables (one per line, comments allowed).
# Checked by: just config check
#
# None: the Rust/Tauri desktop port builds and starts without secrets or
# project-specific environment variables. Optional development switches are
# documented in .env.example.
