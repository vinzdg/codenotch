{
  "identifier": "dropzones",
  "description": "The drop-zone overlay shown while the notch is carried by its handle: it only listens for the zones it draws, so it gets event listening and nothing else (not core:default)",
  "windows": [
    "dropzones"
  ],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
