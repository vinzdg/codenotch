{
  "identifier": "default",
  "description": "Codenotch notch & settings windows: allow core APIs (event.listen is required for state broadcasts), plus dragging and closing the frameless settings window",
  "windows": [
    "notch",
    "settings"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-close"
  ]
}
